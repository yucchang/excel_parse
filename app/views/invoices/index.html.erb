<h1>Invoice List</h1>

<%= link_to 'Upload excel', new_import_invoice_path %>

<table>
  <thead>
    <tr>
      <th>Customer</th>
      <th>Invoice No.</th>
      <th>Date</th>
    </tr>
  </thead>

  <tbody>
   <% @invoices.each do |i| %>
    <tr>
      <td><%= i.customer %></td>
      <td><%= i.invoice_no %></td>
      <td><%= i.date %></td>
    </tr>
    <% end %>
  </tbody>
</table>
